source "https://rubygems.org"

gem "jekyll"
gem "jekyll-remote-theme", group: :jekyll_plugins

# Vendored copy of the remote_theme in _config.yml, used only for local
# preview via _config.local.yml so `jekyll serve` works without network
# access to fetch the remote theme.
gem "jekyll-theme-cayman"
